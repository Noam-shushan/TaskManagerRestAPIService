namespace TaskManager.DataAccess.Models
{
    public enum MissionStatus
    {
        Pending = 1,
        InProgress = 2,
        Done = 3,
        Canceled = 4
    }

    public enum Priority
    {
        Low = 1,
        Middle = 2,
        High = 3
    }
}
